# 更新日志

## [0.1.2] - 2026-08-19

### 新增

- **插件一键更新（插件市场 / 已安装页）**
  - 已安装插件有新版本可用时，按钮由「已安装」变为「更新」，点击直接升级到最新版本。
  - 更新走与安装相同的完整链路：pnpm 拉取最新（git 源自动更新到最新 commit/tag）→ 装后验证 → 软刷新热生效；失败自动 AI 诊断修复或回滚，不留半更新状态。
- **内置默认插件自动安装**
  - 首次启动自动安装随包内置的默认插件（dsh-better-sidebar、dsh-at-file、dsh-token-usage 等 8 个），新装环境开箱即用，插件配置与开发环境一致。
  - 幂等：已安装则跳过，老用户升级不受影响。
- **MCP 自动检测（类似 Claude Code / opencode）**
  - 启动后自动读取用户级 `~/.claude.json` 与 opencode 配置（`~/.config/opencode/opencode.json`/`.jsonc`）的 MCP 并同步到桌面端：stdio（command/args/env）与 HTTP（url/headers）原样映射，opencode `local` 类型自动转换。
  - 同名条目更新为检测值、缺失自动新增；手动配置（context7/burp 等）保留不动；SSE（remote）端点跳过不覆盖手动桥接；变化后热重载立即生效。
- **「链接 / 命令安装」入口移至插件市场**
  - 在插件市场顶部直接输入链接/命令安装插件，不再需要切换页面。

### 优化

- **插件配置模板对齐**
  - 内置 `PROFILE_TEMPLATES.web` 顺序与当前发布环境插件配置完全一致，新装用户与开发环境插件列表一致。

### 修复

- **git 黑框（dsh-better-sidebar）**
  - 修复「源代码管理」触发 git 命令时弹出黑色命令行窗口：git spawn 补充 `windowsHide`。
- **MCP 状态误报「命令未找到」**
  - 修复自动检测写入绝对路径命令后，MCP 列表误报「命令未找到」：`where.exe` 只查 PATH 不支持绝对路径，改为绝对路径直接检查文件存在。

### 其他

- 版本号升级至 0.1.2。

## [0.1.1] - 2026-08-18

### 新增

- **AI 安装（设置 → 已安装插件）**
  - 常规安装失败时**自动接手 AI 诊断**（无需手动操作）：基于失败日志直接诊断 → 白名单修复 → 带参重试，最多 3 轮。
  - AI 建议的参数（`--registry` 等）真实传递给 pnpm（修复参数未生效问题）。
  - 复用当前默认模型服务（`agent-default-model`）的配置与密钥，**不复用视觉模型密钥**。
  - 插件不兼容（能装但加载失败）：回滚后直接明确失败并提示，不再无意义 AI 重试。
  - AI 全失败自动清理残留依赖，不留脏状态。
- **插件任务悬浮面板（右下角）**
  - 安装/卸载/AI 诊断过程实时显示：阶段（安装中/AI 诊断中/回滚中）、加载动画、实时日志（可滚动、自动跟随）。
  - 完成后 30 秒自动收起，支持 ✕ 手动关闭。
- **插件热更新**
  - 安装/卸载插件后自动软刷新（`reloadHarnessSoft`）让插件立即生效，不再弹“重启应用”提示。
  - 后台任务仅显示运行中的任务，完成/失败任务不再常驻列表。
- **归档管理：删除会话**
  - 回滚列表新增「删除」按钮（此前删除功能仅后端存在、前端无入口）：删除后移入回收站可恢复。
  - 回滚/回收站列表每 20 秒自动无感刷新，外部操作自动同步。
- **插件完成/失败提示音**
  - 安装/卸载完成播放成功音（上升双音），失败播放失败音（下降双音）；Web Audio 生成，无需音频文件。
- **设置分区专属图标**
  - 设置侧边栏分区配置专属图标（插件与MCP/归档管理/更新/视觉工具/Token用量），不再全是齿轮。
- **检查更新信息完善**
  - 显示内置版本与内核版本（应用 0.1.1 + 内核 rc.7）。
  - GitHub Releases 尚未发布安装包（HTTP 404）时提示“官方尚未发布安装包”，不再误报“查询失败”。

### 优化

- **内核升级 0.1.0-rc.6 → 0.1.0-rc.7**
  - 53 个直接依赖 + 132 个传递依赖全部升级（共 185 个 @deepseek-ai 包，约 51 个有代码变化）。
  - LLM 层（llm-pi-ai/llm-retry/llm-deepseek）重试机制重构；前端 UI 组件（client-ui-*）大量更新。
  - 升级后全部本地补丁（终端提速、黑框、pnpm shell）重新应用并验证通过。
- **启动提速（方案 A：Harness 驻留）**
  - 退出应用后 Harness 服务驻留 60 秒，期间快速重启直接复用已就绪的服务端口，秒开。
  - 修复 Windows 下 detached 子进程被 Job 回收的问题，驻留进程可被后续实例接管。
- **启动提速（方案 B：V8 编译缓存）**
  - Harness 首次启动后把编译字节码缓存到 `~/.dsh/cache/node-compile`，避免每次冷启动重复解析编译 500MB 依赖。
- **终端命令提速（约 70 倍）**
  - 修复 `dsh-terminal-bash` 与 `dsh-tool-bash-persistent` 提示符不匹配：底层监听 `dsh> ` 而上层实际使用 `__DSH_PERSISTENT_BASH_PROMPT__ `，导致每次命令都要等 3.5 秒静默超时。
  - 修复打包时硬编码的提示符长度（6 改为动态计算），长提示符不再被截断。
  - 极简模式（持久终端）下单次命令从约 3.5 秒降至毫秒级。
- **启动自动修复误判**
  - 修复「每次启动都被误判为异常退出并全量扫描 340 个会话」的问题：运行标记改在判断之后写入，正常退出后启动跳过全量校验。
  - 热启动（复用驻留 Harness）时直接跳过自动修复，进一步加快启动。

### 修复

- **弹出终端黑框**
  - 修复 Harness 调用 pnpm 时因 `stdio: inherit` + `shell: true` 弹出黑色命令行窗口：改为管道捕获并手动转发输出（`windowsHide`）。
  - 修复更新安装器启动时未隐藏控制台窗口的问题。
  - 修复第三方插件（pi-lens / dsh-lens / dsh-better-sidebar）在 LSP 启动、git 命令、工具探测时弹出黑框：所有 spawn 补 `windowsHide`。
- **`dsh:check-update` 重复注册**
  - 修复主进程 IPC 处理器重复注册导致启动报错「Attempted to register a second handler」。
- **插件装后验证与回滚**
  - 安装成功自动重启验证插件能否加载；不兼容插件（如缺少注入声明的第三方插件）自动回滚并明确提示“与当前内核不兼容”，不再让工作区残留脏状态。
  - 安装命令类失败（网络/registry）自动 AI 诊断修复；插件自身不兼容不再无意义 AI 重试。

### 其他

- 版本号升级至 0.1.1，重新打包安装程序。

## [0.1.0] - 2026-08-17

### 新增

- **归档管理（设置页）**
  - 将原来的“对话回滚”和“删除对话”合并为「归档管理」分区。
  - 分区内提供「回滚」「回收站」两个页签。
  - 回收站支持：
    - 查看归档会话列表（仅列出已归档/回收的会话）。
    - 恢复归档会话到正常会话目录。
    - 彻底删除归档会话。
    - 显示回收站磁盘路径，并支持一键打开回收站文件夹。
- **插件市场快照自动更新**
  - 启动后自动拉取最新插件市场列表。
  - 拉取成功后保存到 `~/.dsh/plugin-market-snapshot.md`。
  - 每 6 小时后台自动刷新一次；离线时优先使用最近一次本地快照。
- **插件任务后台运行与通知**
  - 插件安装/卸载改为后台任务，关闭设置页不会中断。
  - 齿轮图标旁显示任务状态，完成/失败时弹出 toast 通知。
  - 已安装插件列表增加“卸载”按钮，覆盖 bundle 插件与 dependencies 插件。
- **视觉 API 快速配置**
  - 在“设置 → 插件与 MCP → 视觉 API”中直接填写 API Key、Base URL、模型、协议和凭据名称。
  - 保存时自动写入 `~/.dsh/.credentials.yaml` 并同步视觉工具设置，一键测试连接。
  - 只对远程视觉工具（看图/OCR/定位等）需要；本地视觉工具无需配置。

### 修复

- **删除会话**
  - 支持删除非 `session-` 前缀的会话目录（如纯 UUID、`od-` 前缀）。
  - 删除当前会话后，清除已失效的当前会话记录并刷新页面，避免输入框不可用。
  - 删除操作改为移入 `~/.dsh/sessions-trash`，可恢复。
- **会话滚动跳动**
  - `dsh-enhance` 的折叠扫描由每帧改为约 80ms 节流一次，降低流式渲染期间重建 DOM 的频率，缓解页面在会话中频繁往上跳动。
- **消息旁“回滚到此消息”**
  - 只有该用户消息之后还有内容时才显示“回滚到此消息”按钮；如果它已经是最后一条消息，不再显示无法执行的撤销按钮，避免“撤销不了”的困惑。
- **聊天间距 / Deep diving 大段空白**
  - 核心：`dsh-client-ui-conversation` 不再渲染“无任何可见内容”的 assistant-step 空节点，避免零高度节点在 flex 布局中产生多余间距。
  - 本地 `dsh-enhance` 插件：
    - 空节点判断改为“实际可见矩形（宽高 > 0）”，避免把零高度空壳误判为可见。
    - 展开 Think/工具时清除内联 `display:none`，修复“Think 无法展开”。
    - 收起后重新隐藏空节点，避免展开/收起后出现大段空白。
  - 注：`dsh-enhance` 为本地第三方插件，修复仅在本机 `~/.dsh/profiles/web/node_modules/dsh-enhance` 中生效，不随安装包分发。
- **暗色主题**
  - 设置页、归档管理、插件管理相关样式改用 DSH 主题变量，暗色主题下可读。
- **启动速度**
  - `dsh:restart` 重启应用前主动清除“正在运行”标记，避免下次启动被误判为异常退出而全量校验会话日志。
  - 启动时并行探测已有 `dsh web` 服务与初始化桌面设置插件，减少串行等待。
  - 会话列表改为后台异步扫描 + 持久化摘要缓存，设置页首次打开不再卡死主进程。
- **插件卸载**
  - 修复 git 源插件卸载失败问题：仅在 pnpm 卸载成功后同步 bundle 配置。
  - 仅存在于 bundle 层、不在 dependencies 中的插件，直接移除 bundle 配置，无需 pnpm remove。
  - 卸载失败时暂停 Harness 重试一次，并自动恢复服务。
- **插件安装**
  - 安装改为后台执行，超时自动终止并返回日志。
  - 支持 `github:`、`git+https:`、`git+ssh:`、`https://...git`、GitHub 归档直链等安装源。

### 其他

- 重新打包安装程序：
  - `dist\DeepSeek Harness Setup 0.1.0.exe`
  - 打包流程修复：`d3dcompiler_47.dll` 先以别名暂存，NSIS 安装时恢复原名。
- 用户数据安全：密钥等敏感数据保存在 `~/.dsh`，不随安装包分发。
